# MVP rules

See `docs/MVP-FORMULA.md` for the default formula.

Custom batting / bowling / fielding points live on `TournamentRuleSet.mvpJson`. The match uses the snapshotted version after innings start. Super Stars and the statistics MVP tab show a Points System sheet (not raw JSON).
